---
# .ansible-lint
# Configuration for ansible-lint in CI Quality Gate

exclude_paths:
  - ansible/roles/
  - .cache/
  - mkdocs.yml

skip_list:
  - yaml  # Handled authoritatively in CI by dedicated yamllint step using .yamllint.yml
